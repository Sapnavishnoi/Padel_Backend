const express = require('express');
const authController = require('../controllers/auth')

const app = express();


// app.use('/api', (req, res) =>  authController.register);

//app.use('/api', authController.register);
//app.use('/api', authController.login);